# Development dependencies for threat-modeling-py.
# The package itself should be installed with `pip install -e .` or `pip install -e ".[dev]"`.
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
mypy>=1.0.0
ruff>=0.1.0
